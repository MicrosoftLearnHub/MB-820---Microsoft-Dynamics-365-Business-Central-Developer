# Domain 1: Business Central Architecture & AL Extensions

- Cloud architecture, AppSource vs Per-tenant extensions (PTE), extension lifecycle.
- Project configuration (`app.json`, `launch.json`), target runtimes, dependencies, ID ranges.
- Breaking changes, data upgrade codeunits, and schema synchronization modes.